{
  "name": "jobboard-proxy",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker proxy for scraping job boards (JobsDB, CTgoodjobs, Indeed, OfferToday) — used by the Azure Functions scraper.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "types": "wrangler types",
    "test": "echo \"No tests configured yet\" && exit 0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260811.1",
    "typescript": "^5.5.0",
    "wrangler": "^4.123.0"
  }
}
